{
    "name": "kamstrup-mbus-mqtt devcontainer",
    "image": "ghcr.io/cachix/devenv/devcontainer:latest",
    "forwardPorts": [],
    "onCreateCommand": "sudo chown -R vscode:vscode /nix /home/vscode/.cache /home/vscode/.local 2>/dev/null || true",
    "updateContentCommand": "cd /workspaces/kamstrup-mbus-mqtt && devenv shell -- true",
    "postCreateCommand": "cd /workspaces/kamstrup-mbus-mqtt && direnv allow",
    "remoteEnv": {
        "DIRENV_LOG_FORMAT": ""
    },
    "customizations": {
        "vscode": {
            "settings": {
                // format on save kann performance verschlechtern (da ist dann Eclipse eine Rakete gegen VS Code ;-) )
                "editor.formatOnSave": false,
                "intellijFormatter.formatOnSave": false,
                "editor.formatOnSaveMode": "modifications",
                "editor.defaultFormatter": "idea-formatter.intellij-formatter",
                "direnv.restart.automatic": true,
                "terminal.integrated.defaultProfile.linux": "bash"
            },
            "extensions": [
                "mkhl.direnv",
                "redhat.vscode-quarkus",
                "alphabotsec.vscode-eclipse-keybindings",
                "asciidoctor.asciidoctor-vscode",
                "jebbs.plantuml",
                "vscjava.vscode-java-pack",
                "oleksandrhavrysh.intellij-formatter",
                "sonarsource.sonarlint-vscode"
            ]
        }
    }
}
